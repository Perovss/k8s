---
apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: {{ctx.namespace}}
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: {{gitea.gitea_port1}}
    targetPort: {{gitea.gitea_port1}}
  selector:
    app: gitea
